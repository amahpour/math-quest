{
  "name": "math-quest",
  "version": "0.1.0",
  "private": true,
  "description": "Test harness for the math-quest static site (Playwright only — the app itself has no build step).",
  "scripts": {
    "serve": "python3 -m http.server 8765 --directory app",
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
